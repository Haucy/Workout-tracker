{
  "name": "worktrack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Workout tracker — Supabase + Gemini AI",
  "scripts": {
    "build:config": "node generate-config.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}